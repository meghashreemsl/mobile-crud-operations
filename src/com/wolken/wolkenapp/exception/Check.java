package com.wolken.wolkenapp.exception;

/*
public class Check {
	public void check(int price) throws MobileException {
		if(price!=0 && price >0) {
			System.out.println("price is proper");
		}
		else {
			throw new MobileException();
		}
	}
	*/
	
	/*
	 * public MobileDTO[] mobileDTO; public int index;
	 * 
	 * public void Mobile(int size) { mobileDTO = new MobileDTO[size]; } public void
	 * checkPrice() throws MobileException { for(int i=0; i < mobileDTO.length; i++)
	 * { if(mobileDTO[i] != null) { if(mobileDTO[i].getPrice() <0) { throw new
	 * MobileException(); }else { System.out.println("price is valid"); } } else {
	 * System.out.println("details not found"); } } }
	 *

}*/
